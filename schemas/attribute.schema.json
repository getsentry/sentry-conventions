{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Sentry Span Attribute Schema",
  "description": "Describes an attribute and how it's meant to be used",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "key": {
      "description": "The name of the attribute, for ex. http.route",
      "type": "string"
    },
    "brief": {
      "description": "A description of the attribute",
      "type": "string"
    },
    "has_dynamic_suffix": {
      "description": "If an attribute has a dynamic suffix, for example http.response.header.<key> where <key> is dynamic",
      "type": "boolean"
    },
    "type": {
      "description": "The type of the attribute value",
      "enum": ["string", "boolean", "integer", "double", "string[]", "boolean[]", "integer[]", "double[]", "any"]
    },
    "apply_scrubbing": {
      "description": "How PII scrubbing should be applied to the attribute value. 'auto' scrubs automatically, 'manual' requires manual scrubbing rules, 'never' is never scrubbed. Optionally include a reason about why this mode applies",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "key": {
          "enum": ["auto", "manual", "never"]
        },
        "reason": {
          "type": "string"
        }
      },
      "required": ["key"]
    },
    "is_in_otel": {
      "type": "boolean"
    },
    "visibility": {
      "description": "The visibility of the attribute",
      "enum": ["public", "internal"]
    },
    "example": {
      "description": "An example value of the attribute",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "boolean"
        },
        {
          "type": "number"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "type": "array",
          "items": {
            "type": "boolean"
          }
        },
        {
          "type": "array",
          "items": {
            "type": "number"
          }
        }
      ]
    },
    "migration": {
      "description": "Data migrations that this attribute participates in. Migration definitions live in model/attribute_migrations/.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "source_for": {
          "description": "Migrations that consume this attribute as a source.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "uniqueItems": true
        },
        "target_of": {
          "description": "Migrations that produce this attribute as a target.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "uniqueItems": true
        }
      },
      "anyOf": [{ "required": ["source_for"] }, { "required": ["target_of"] }]
    },
    "deprecation": {
      "description": "If an attribute was deprecated, and what it was replaced with",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "replacement": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        },
        "_status": {
          "description": "How the attribute should be handled in the ingestion pipeline. 'backfill' copies the value to the new name. 'normalize' moves the value to the new name. In both cases, existing values for the new name will not be overwritten. The attribute should be backfilled for 90 days before status is set to normalize.",
          "enum": [null, "backfill", "normalize"]
        }
      },
      "required": ["_status"]
    },
    "alias": {
      "description": "If there are attributes that alias to this attribute",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "additional_context": {
      "description": "A list of freeform notes providing additional context about how this attribute behaves, common pitfalls, or query-time nuances",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "changelog": {
      "description": "A list of changelog entries tracking how this attribute has changed across versions",
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "version": {
            "description": "The sentry-conventions release version",
            "type": "string"
          },
          "prs": {
            "description": "GitHub PR numbers",
            "type": "array",
            "items": {
              "type": "integer"
            }
          },
          "description": {
            "description": "Optional description of what changed",
            "type": "string"
          }
        },
        "required": ["version"]
      }
    }
  },
  "required": ["key", "brief", "type", "apply_scrubbing", "is_in_otel", "visibility"]
}

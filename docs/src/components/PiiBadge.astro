---
interface Props {
  pii: 'true' | 'maybe' | 'false';
  reason?: string;
}

const { pii, reason } = Astro.props;

const labels = {
  true: 'Has PII',
  maybe: 'Maybe PII',
  false: 'No PII',
};

const colorClasses = {
  true: 'bg-pii-true/15 text-pii-true',
  maybe: 'bg-pii-maybe/15 text-pii-maybe',
  false: 'bg-pii-false/15 text-pii-false',
};
---

<span 
  class:list={[
    "inline-flex items-center px-2 py-1 text-xs font-medium rounded-sm uppercase tracking-wide",
    colorClasses[pii]
  ]} 
  title={reason}
>
  {labels[pii]}
</span>
